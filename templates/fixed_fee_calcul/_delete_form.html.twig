<form method="post" action="{{ path('app_fixed_fee_calcul_delete', {'id': fixed_fee_calcul.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fixed_fee_calcul.id) }}">
    <button class="btn">Delete</button>
</form>
