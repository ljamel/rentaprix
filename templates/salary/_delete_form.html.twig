<form method="post" action="{{ path('app_salary_delete', {'id': salary.id}) }}" onsubmit="return confirm('êtes vous sûr de vouloir supprimer cet élement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salary.id) }}">
    <button class="delete-btn">Supprimer</button>
</form>
