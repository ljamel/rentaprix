<form method="post" action="{{ path('app_variable_fee_delete', {'id': variable_fee.id}) }}" onsubmit="return confirm('êtes vous sûr de vouloir supprimer cet élement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ variable_fee.id) }}">
    <button class="delete-btn">Supprimer</button>
</form>
