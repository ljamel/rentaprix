<form method="post" action="{{ path('app_variable_fee_delete', {'id': variable_fee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ variable_fee.id) }}">
    <button class="btn">Delete</button>
</form>
