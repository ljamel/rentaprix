{% extends 'base-membre.html.twig' %}

{% block title %}VariableFee{% endblock %}

{% block body %}
    <h1>VariableFee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ variable_fee.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ variable_fee.title }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ variable_fee.price }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ variable_fee.unit }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ variable_fee.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_variable_fee_index') }}">back to list</a>

    <a href="{{ path('app_variable_fee_edit', {'id': variable_fee.id}) }}">edit</a>

    {{ include('variable_fee/_delete_form.html.twig') }}
{% endblock %}
